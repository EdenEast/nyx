# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/build-nix.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}
jobs:
  build-package-fwhich:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 1
    - uses: DeterminateSystems/nix-installer-action@main
    - uses: cachix/cachix-action@master
      with:
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        name: edeneast
    - name: Build package fwhich
      run: nix build --accept-flake-config --print-out-paths --show-trace .#packages.x86_64-linux.fwhich
  build-package-space-sanitize:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 1
    - uses: DeterminateSystems/nix-installer-action@main
    - uses: cachix/cachix-action@master
      with:
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        name: edeneast
    - name: Build package space-sanitize
      run: nix build --accept-flake-config --print-out-paths --show-trace .#packages.x86_64-linux.space-sanitize
name: build-nix
'on':
  pull_request: {}
  push:
    branches:
    - main
    paths:
    - flake.lock
    - flake.nix
    - packages/
  workflow_dispatch: {}
