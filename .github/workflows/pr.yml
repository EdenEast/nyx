name: Pull Request
on:
  pull_request:
    paths-ignore:
      - "**.md"
      - "**.org"
      - "**.norg"
      - "bin/**"
      - "docs/**"

jobs:
  build:
    if: github.event.pull_request.draft == false
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: pride
          - os: ubuntu-latest
            target: sloth
          - os: ubuntu-latest
            target: eden
          - os: macos-latest
            target: theman
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Install nix
        uses: cachix/install-nix-action@v15
        with:
          install_url: https://github.com/numtide/nix-flakes-installer/releases/download/nix-2.4pre20210604_8e6ee1b/install
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}

      - uses: cachix/cachix-action@v10
        with:
          name: edeneast
          skipPush: true
          extraPullNames: nix-community

      - name: Build pr target
        env:
          TARGET: ".#top.${{ matrix.target }}"
        run: nix build $TARGET --show-trace --fallback -v --log-format raw

      - name: Fetch current main and build it in a profile
        env:
          TARGET: "github:EdenEast/nyx#top.${{ matrix.target }}"
        run: nix build $TARGET -v --log-format raw --profile ./profile

      - name: Apply new target to profile
        env:
          TARGET: ".#top.${{ matrix.target }}"
        run: nix build $TARGET -v --log-format raw --profile ./profile

      - name: Diff profile
        id: diff
        run: |
          diff="$(nix profile diff-closures --profile ./profile)"
          echo "$diff"
          diff="$(echo "$diff" | sed 's/\x1b\[[0-9;]*m//g')"
          diff="${diff//'%'/'%25'}"
          diff="${diff//$'\n'/'%0A'}"
          diff="${diff//$'\r'/'%0D'}"
          echo "::set-output name=diff::$diff"

      - name: Scan for security issues
        id: security
        run: |
          nix run nixpkgs/nixos-20.09#vulnix -- -w https://raw.githubusercontent.com/ckauhaus/nixos-vulnerability-roundup/master/whitelists/nixos-20.09.toml ./profile | tee /tmp/security.txt
          OUTPUT_SECURITY="$(cat /tmp/security.txt)"
          OUTPUT_SECURITY="${OUTPUT_SECURITY//'%'/'%25'}"
          OUTPUT_SECURITY="${OUTPUT_SECURITY//$'\n'/'%0A'}"
          OUTPUT_SECURITY="${OUTPUT_SECURITY//$'\r'/'%0D'}"
          echo "$OUTPUT_SECURITY"
          echo "::set-output name=security::$OUTPUT_SECURITY"

      - name: Comment report in pr
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: ".#top.${{ matrix.target }}"
          message: |
            ### Report for `${{ matrix.target }}`
            Version changes:
            ```
            ${{ steps.diff.outputs.diff }}
            ```
            <details>
            <summary>
            Security vulnerability report
            </summary>
            <br>
            <pre>
            ${{ steps.security.outputs.security }}
            </pre>
            </details>

# Idea reference
# https://github.com/arianvp/nixos-stuff/blob/adec9e6/.github/workflows/pr.yml
# https://github.com/sandhose/nixconf/blob/055de30/.github/workflows/check-pr.yaml
